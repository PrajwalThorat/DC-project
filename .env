# DC Projects - Environment (copy of .env.example with placeholders filled)
# Edit values as needed. Keep this file private and DO NOT commit to Git.

# Application
DC_SECRET_KEY=change_this_to_a_strong_secret
ENVIRONMENT=production
DEBUG=False

# Database (MySQL)
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Pass@123
DB_NAME=dc_projects

# Project paths
# Path where project folders will be stored on the server. Use absolute path.
DC_PROJECTS_ROOT=/path/to/projects

# Optional: UNC path (Windows)
# DC_PROJECTS_ROOT=\\192.168.1.100\Data
